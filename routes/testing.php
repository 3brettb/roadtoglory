<?php

Route::get('/test', function(){
    
    //$trade->picks()->attach(\App\Models\DraftPick::find(2), ['team_id' => 1]);
    //$trade->players()->attach(\App\Models\Player::find(2), ['team_id' => 1]);
    //$trade->teams()->attach(\App\Models\Team::find(2), ['team_id' => 1]);
    //$trade->picks()->attach(\App\Models\DraftPick::find(1), ['team_id' => 2]);
    //$trade->players()->attach(\App\Models\Player::find(1), ['team_id' => 2]);
    //$trade->teams()->attach(\App\Models\Team::find(1), ['team_id' => 2]);
    
    //$draft = \App\Models\Draft::find(1);
    //$done = \App\Helpers\Admin\Generate::DraftPicks($draft);
    //dd($done);

    //dd(user()->permissions->can('ADMIN'));

});